import { type Config } from "prettier";

export default {
    tabWidth: 4,
    printWidth: 120,
    semi: true,
    singleQuote: false,
    trailingComma: "all",
    arrowParens: "avoid",
} satisfies Config;
